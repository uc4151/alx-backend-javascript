export default function getResponseFromAPI() {
//exporting the function so it can be imported
    return new Promise((resolve, reject) => { 
        // resolving the promise
        resolve();
    });
}